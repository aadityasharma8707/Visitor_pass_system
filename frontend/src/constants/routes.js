/**
 * Route path constants.
 * Centralizing route strings prevents typos and makes future
 * route restructuring a single-file change.
 */
const Routes = Object.freeze({
  VISITOR:  "/",
  HOST:     "/host",
  SECURITY: "/security",
  ADMIN:    "/admin",
});

export default Routes;
